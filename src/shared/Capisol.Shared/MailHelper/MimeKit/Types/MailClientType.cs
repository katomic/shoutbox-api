namespace Capisol.Shared.MailHelper.MimeKit.Types
{
    public enum MailClientType { 
        SaveToDirectory, 
        SmtpClient, 
        ImapClient, 
        Pop3Client,
        SendGridClient
    }
}
